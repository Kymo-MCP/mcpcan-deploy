{{- if .Values.services.authz.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.authz.name | default "mcp-authz" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.services.authz.name | default "mcp-authz" }}
    app.kubernetes.io/name: {{ .Values.services.authz.name | default "mcp-authz" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: authz
    app.kubernetes.io/version: {{ .Values.global.version }}
    {{- include "mcpcan.labels" . | nindent 4 }}
  annotations:
    deployment.kubernetes.io/revision: "{{ .Release.Revision }}"
spec:
  replicas: {{ .Values.services.authz.replicas | default 1 }}
  selector:
    matchLabels:
      app: {{ .Values.services.authz.name | default "mcp-authz" }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.authz.name | default "mcp-authz" }}
        app.kubernetes.io/name: {{ .Values.services.authz.name | default "mcp-authz" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: authz
        app.kubernetes.io/version: {{ .Values.global.version }}
        {{- include "mcpcan.selectorLabels" . | nindent 8 }}
      annotations:
        mcpcan.io/version: {{ .Values.global.version }}
        mcpcan.io/revision: "{{ .Release.Revision }}"
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      initContainers:
      - name: wait-deps
        image: {{ if .Values.global.cn }}{{ .Values.global.registryForMirrorCN }}{{ else }}{{ .Values.global.registry }}{{ end }}/{{ .Values.global.images.busybox }}
        command:
        - sh
        - -c
        - >-
          until nc -z {{ .Values.infrastructure.mysql.service.name | default "mysql" }} {{ .Values.infrastructure.mysql.service.port | default 3306 }}; do echo "waiting for mysql"; sleep 2; done;
          until nc -z {{ .Values.infrastructure.redis.service.name | default "redis" }} {{ .Values.infrastructure.redis.service.port | default 6379 }}; do echo "waiting for redis"; sleep 2; done
      containers:
      - name: {{ .Values.services.authz.name | default "mcp-authz" }}
        image: {{ if .Values.global.cn }}{{ .Values.global.registryForMirrorCN }}{{ else }}{{ .Values.global.registry }}{{ end }}/{{ .Values.global.images.mcpAuthz }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        {{- if .Values.services.authz.service.port }}
        ports:
        - containerPort: {{ .Values.services.authz.service.port }}
          protocol: TCP
        {{- end }}
        {{- if .Values.services.authz.resources }}
        resources:
          {{- toYaml .Values.services.authz.resources | nindent 10 }}
        {{- end }}
        volumeMounts:
        - name: config-volume
          mountPath: "/app/config"
          readOnly: true
        - name: data-volume
          mountPath: {{ .Values.global.mountStorage.rootPath | default "/data/mcpcan" }}
      volumes:
      - name: config-volume
        configMap:
          name: mcp-config
          items:
          - key: authz.yaml
            path: authz.yaml
      - name: data-volume
        hostPath:
          path: {{ .Values.global.hostStorage.rootPath | default "/data/mcpcan" }}
          type: DirectoryOrCreate
---
{{- if .Values.services.authz.service.port }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.authz.service.name | default "mcp-authz-svc" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.services.authz.name | default "mcp-authz" }}
    {{- include "mcpcan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.services.authz.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.services.authz.service.port }}
    targetPort: {{ .Values.services.authz.service.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.services.authz.name | default "mcp-authz" }}
{{- end }}
---
{{- if and .Values.ingress.enabled .Values.services.authz.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.services.authz.name | default "mcp-authz" }}-ingress
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.services.authz.name | default "mcp-authz" }}
    {{- include "mcpcan.labels" . | nindent 4 }}
  {{- $ann := (merge (.Values.services.authz.ingress.annotations | default dict) (.Values.ingress.annotations | default dict)) }}
  {{- if gt (len $ann) 0 }}
  annotations:
    {{- toYaml $ann | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
    {{- if .Values.global.domain }}
    - {{ .Values.global.domain }}
    {{- end }}
    secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
  {{- if .Values.global.domain }}
  - host: {{ .Values.global.domain }}
  {{- else }}
  - 
  {{- end }}
    http:
      paths:
      - path: {{ .Values.services.authz.ingress.path | default "/" }}
        pathType: {{ .Values.services.authz.ingress.pathType | default "Prefix" }}
        backend:
          service:
            name: {{ .Values.services.authz.service.name | default "mcp-authz-svc" }}
            port:
              number: {{ .Values.services.authz.service.port }}
{{- end }}
{{- end }}
